{
  "name": "backend",
  "scripts": {
    "dev": "uvicorn main:app --reload",
    "start": "uvicorn main:app --reload",
    "start:prod": "export ENV=production && uvicorn main:app -w 4 -k uvicorn.workers.UvicornWorker",
    "migrate": "alembic upgrade head",
    "test": "pytest"
  }
}
